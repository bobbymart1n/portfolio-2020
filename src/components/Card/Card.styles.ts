import styled from 'styled-components';

const StyledCard = styled.div`
  width: 100%;
`;

const StyledCardHeading = styled.h3``;

const StyledCardDate = styled.p`
  font-weight: 100;
  letter-spacing: 1px;
`;

export { StyledCard, StyledCardHeading, StyledCardDate };
