import styled from 'styled-components';

const StyledPortfolioPiece = styled.div``;

const StyledPortfolioPieceImage = styled.img`
  max-width: 100%;
`;

export { StyledPortfolioPiece, StyledPortfolioPieceImage };
